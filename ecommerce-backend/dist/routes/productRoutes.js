import express from "express";
import { adminOnly } from "../middlewares/auth.js";
import { deleteProduct, getAllCategories, getAllProducts, getAllProductsWithFilters, getLatestProduct, getProductByID, newProduct, updateProduct, generateRandomProducts } from "../controller/productController.js";
import { singleUpload } from "../middlewares/multer.js";
const app = express.Router();
app.post("/new", adminOnly, singleUpload, newProduct);
app.get("/latest", getLatestProduct);
app.get("/categories", getAllCategories);
app.get("/admin-products", adminOnly, getAllProducts);
app.get("/serach-products", adminOnly, getAllProductsWithFilters);
app.get("/getProduct/:id", getProductByID);
app.post("/generate-random-products", generateRandomProducts);
app.put("/update-product/:id", adminOnly, singleUpload, updateProduct);
// app.put("/updateProduct/:id",adminOnly, singleUpload, updateProduct);
app.delete("/delete-product/:id", adminOnly, deleteProduct);
// app.delete("/deleteProduct/:id",adminOnly, deleteProduct);
export default app;
